fn capture(count: int) {
	print count;
	print capture;

	if (count < 5) {
		return count;
	}

	print count;
	print capture;

	count++;
	count++;

//	return capture(count + 1);
	return -1;
}

print capture(52);
