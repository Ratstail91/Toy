import fileio;

// test open
{
    var file = open("scripts:/fileio.txt", "r");

    // file.read(string);
    
    // TODO:
    // file.write(12, ",", 12);
    // file.size();
    // file.isOpened();
    // file.error();
    // file.position();
    // file.mode();
    
    // close(file);
}