import fileio;

// test open
{
    var file = open("scripts:/lib/file/fileio.txt", "r");

    // file.read(string);
    
    // TODO:
    // file.write(12, ",", 12);
    // file.size();
    // file.isOpened();
    // file.getrror();
    // file.position();
    // file.mode();
    
    print file.close();
}